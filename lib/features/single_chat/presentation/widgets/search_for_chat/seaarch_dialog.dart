import 'package:flutter/material.dart';

class SeaarchDialog extends StatelessWidget {
  const SeaarchDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
